# Зміни реалізовані в цьому PR

## Огляд

Цей PR реалізує чотири ключові покращення для бота моніторингу електроенергії:

1. **Жирний текст у повідомленнях про світло**
2. **Адмін команда `/setinterval`**
3. **Запобігання дублікатам публікацій при перезапуску**
4. **Покращена логіка перевірки графіка**

## Детальний опис змін

### 1. Форматування повідомлень про світло з жирним текстом

**Файли:** `src/powerMonitor.js`

**Що змінено:**
- Додано `<b>` теги навколо часу в повідомленнях про зникнення/появу світла
- Додано `<b>` теги навколо планового часу у графіку

**Приклад виводу:**
```
🔴 22:56 Світло зникло    →    🔴 <b>22:56 Світло зникло</b>
🕓 Воно було 1 хвилина           🕓 Воно було 1 хвилина
🗓 Наступне планове: 02:00 - 09:00    →    🗓 Наступне планове: <b>02:00 - 09:00</b>

🟢 22:57 Світло з'явилося    →    🟢 <b>22:57 Світло з'явилося</b>
🕓 Його не було менше хвилини       🕓 Його не було менше хвилини
🗓 Наступне планове: 02:00 - 09:00    →    🗓 Наступне планове: <b>02:00 - 09:00</b>
```

### 2. Команда `/setinterval` для адмінів

**Файли:** 
- `src/handlers/admin.js` - обробник команди
- `src/bot.js` - реєстрація команди
- `src/database/db.js` - таблиця налаштувань
- `src/config.js` - читання з БД

**Функціонал:**
- Дозволяє адміністраторам змінювати інтервали перевірки
- Зберігає налаштування в базі даних (таблиця `settings`)
- Валідація значень з встановленими лімітами

**Використання:**
```bash
/setinterval schedule 300  # Інтервал перевірки графіка (секунди)
/setinterval power 5       # Інтервал моніторингу світла (секунди)
```

**Ліміти:**
- Перевірка графіка: 5-3600 секунд (5 сек - 60 хв)
- Моніторинг світла: 1-60 секунд

**Важливо:** Після зміни інтервалів потрібен перезапуск бота для застосування змін.

### 3. Запобігання дублікатам публікацій

**Файли:**
- `src/database/db.js` - додано поле `last_published_hash`
- `src/database/users.js` - функції для роботи з хешами
- `src/scheduler.js` - логіка перевірки

**Що реалізовано:**
- Додано окреме поле `last_published_hash` (окремо від `last_hash`)
- `last_hash` - останній перевірений хеш графіка
- `last_published_hash` - хеш останнього опублікованого графіка
- Публікація відбувається тільки якщо `currentHash !== last_published_hash`

**Логіка роботи:**
```javascript
// Перевірка 1: Чи змінився хеш з останньої публікації?
if (currentHash === user.last_published_hash) {
  // Графік не змінився, не публікуємо (запобігає дублікатам при перезапуску)
  updateUserHash(user.id, currentHash);  // Оновлюємо тільки last_hash
  return;
}

// Перевірка 2: Чи змінився хеш з останньої перевірки?
if (currentHash === user.last_hash) {
  // Хеш не змінився з останньої перевірки
  return;
}

// Публікуємо графік
publishSchedule();

// Оновлюємо обидва хеші
updateUserHashes(user.id, currentHash);
```

**Нові функції в `src/database/users.js`:**
- `updateUserPublishedHash(id, hash)` - оновити тільки last_published_hash
- `updateUserHashes(id, hash)` - оновити обидва хеші одночасно

### 4. Покращена логіка планувальника

**Файли:** `src/scheduler.js`

**Покращення:**
- Реалізовано двоступеневу перевірку хешів
- При помилці публікації оновлюється тільки `last_hash`, а не `last_published_hash`
- Це дозволяє повторити спробу публікації після усунення проблеми

**Сценарії:**

1. **Перезапуск бота (графік не змінився):**
   - `currentHash == last_published_hash` → не публікуємо
   - Оновлюємо `last_hash`, залишаємо `last_published_hash` як є

2. **Графік змінився:**
   - `currentHash != last_published_hash` → публікуємо
   - Оновлюємо обидва хеші після успішної публікації

3. **Помилка публікації:**
   - Оновлюємо тільки `last_hash`
   - При наступній перевірці спробуємо опублікувати знову

## Міграція бази даних

**Автоматичні зміни при запуску:**
1. Додається колонка `last_published_hash` до таблиці `users`
2. Створюється таблиця `settings` для зберігання налаштувань

**SQL:**
```sql
-- Користувачі
ALTER TABLE users ADD COLUMN last_published_hash TEXT;

-- Налаштування
CREATE TABLE settings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT UNIQUE NOT NULL,
  value TEXT NOT NULL,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## Тестування

Всі зміни протестовані:
- ✅ Синтаксичні перевірки всіх файлів
- ✅ Існуючі тести проходять успішно
- ✅ Додаткові тести для нових функцій
- ✅ Перевірка структури бази даних
- ✅ Перевірка функцій оновлення хешів
- ✅ Перевірка таблиці налаштувань

## Зворотна сумісність

✅ Всі зміни зворотно сумісні:
- Існуючі користувачі автоматично отримають нове поле `last_published_hash`
- Якщо налаштування не знайдені в БД, використовуються значення з `.env`
- Старі функції продовжують працювати

## Використання після розгортання

1. **Налаштування інтервалів:**
   ```bash
   /setinterval schedule 180  # Перевірка графіка кожні 3 хвилини
   /setinterval power 10      # Перевірка світла кожні 10 секунд
   ```

2. **Перезапуск для застосування:**
   - Railway автоматично перезапустить бота при push
   - Або вручну через панель Railway

3. **Перевірка:**
   - Перегляньте логи для підтвердження нових інтервалів
   - Перевірте, що дублікати графіків не публікуються при перезапуску

## Важливі примітки

⚠️ **Після зміни інтервалів потрібен перезапуск бота**

⚠️ **Команда `/setinterval` доступна тільки адміністраторам** (встановлені в `ADMIN_IDS`)

✅ **Міграції запускаються автоматично** при старті бота

✅ **Налаштування зберігаються між перезапусками** в базі даних
